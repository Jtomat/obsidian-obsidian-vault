
```python
class ConditionalNode(Node):
    type: Literal["conditional"] = "conditional"

    condition: Expression
    then: Expression
    else_: Expression = Field(alias="else") # может убрать?
```

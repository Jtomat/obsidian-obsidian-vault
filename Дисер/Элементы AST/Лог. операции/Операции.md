\- по умолчанию все операции проводятся над `integer` (в виде 0 | 1)

```python
LogicalOperator = Literal[
    "and",
    "or",
    "not"
]


class LogicalOperationNode(Node):
    type: Literal["logical_operation"] = "logical_operation"

    operator: LogicalOperator

    left: Expression
    right: Expression
```